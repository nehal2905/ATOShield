# Keep these in sync with backend/requirements.txt so the model that is trained
# here unpickles in the backend runtime (same-or-older sklearn at serve time).
numpy>=2.1,<3
pandas>=2.2.3,<3
scikit-learn>=1.9,<2
joblib>=1.4
scipy>=1.13
shap>=0.46
pyarrow>=16
matplotlib>=3.9
pytest>=8.2
